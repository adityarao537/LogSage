# How to Change the Logo

1. Prepare your logo image file (SVG, PNG, or JPG).
2. Rename your image file to `logo.svg` (or update `frontend/index.html` and `frontend/style.css` to match your file extension).
3. Replace the existing `logo.svg` in this folder with your new file.
4. Refresh the application to see the changes.
